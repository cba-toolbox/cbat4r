% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/questionnaire.R
\name{set_qnr}
\alias{set_qnr}
\title{Create a jsPsych Likert questionnaire task}
\usage{
set_qnr(
  task_name = "scale_name",
  scale,
  item,
  instruction = "",
  randomize_order = FALSE,
  jsPsych_version = "8.2.2",
  output_dir = ".",
  add_root_dir = TRUE,
  overwrite = FALSE
)
}
\arguments{
\item{task_name}{Name of the task, used as the directory name. Must be a
path-safe name (letters, digits, \verb{_}, \code{-}, \code{.}).}

\item{scale}{Character vector of scale labels
(e.g. \code{c("Strongly Disagree", ..., "Strongly Agree")}).}

\item{item}{Data frame of questionnaire items with columns \code{prompt}
(question text) and \code{name} (variable name); optionally \code{required}
(\code{TRUE}/\code{FALSE} or \code{"true"}/\code{"false"}, default \code{TRUE}) and \code{labels}
(name of the JS variable holding the scale labels, default \code{"scale"}).}

\item{instruction}{Instruction or preamble text displayed above the items.}

\item{randomize_order}{Whether to randomize the item order
(\code{TRUE}/\code{FALSE} or \code{"true"}/\code{"false"}).}

\item{jsPsych_version}{jsPsych version to use (e.g. \code{"6.3.1"}, \code{"7.3.4"},
\code{"8.2.2"}). Any 7.1+ release works, including releases newer than this
package.}

\item{output_dir}{Directory in which the task directory is created.
Defaults to the current working directory.}

\item{add_root_dir}{If \code{TRUE} (the default), a root directory named
\code{task_name} is created inside \code{output_dir} and the HTML entry points and
the \code{task_name} task directory are placed in it. If \code{FALSE}, they are
placed directly in \code{output_dir} instead. Either way, if a directory named
\code{task_name} already exists at the target location, an error is raised
(unless \code{overwrite = TRUE}).}

\item{overwrite}{If \code{TRUE}, an existing task directory is replaced.}
}
\value{
(Invisibly) the same path list as \code{\link[=set_cbat]{set_cbat()}}.
}
\description{
Creates a CBAT task skeleton (see \code{\link[=set_cbat]{set_cbat()}}) whose \code{task.js} presents
the given items with the \code{jsPsychSurveyLikert} plugin.
}
\examples{
\dontrun{
items <- data.frame(
  prompt = c("I feel happy.", "I feel energetic."),
  name = c("happy", "energetic")
)
set_qnr(
  task_name = "mood_survey",
  scale = c("Strongly Disagree", "Disagree", "Neutral", "Agree", "Strongly Agree"),
  item = items,
  instruction = "Please answer the following questions."
)
}
}
